<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dimas.rumahmakan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.routingActivity.RoutingActivity"></activity>
        <activity android:name=".ui.activity.searchRestaurantActivity.SearchRestaurantActivity" />
        <activity android:name=".ui.activity.detailRestaurantActivity.DetailRestaurantActivity" />
        <activity android:name=".ui.activity.exploreActivity.ExploreActivity" />
        <activity android:name=".ui.activity.splashActivity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.here.sdk.access_key_id"
            android:value="@string/here_access_key_id" />
        <meta-data
            android:name="com.here.sdk.access_key_secret"
            android:value="@string/here_access_key_secret" />
    </application>

</manifest>